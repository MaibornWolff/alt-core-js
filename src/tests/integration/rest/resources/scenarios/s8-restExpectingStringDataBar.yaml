description: 'A simple scenario for testing rest functionality. It expects the response to be valid and to contain "bar" as string body.'
actions:
    - name: rest
      responseValidation:
          - "res.toString() === 'bar'"
