import "proto/other.proto"; // test relative-to-root path resolution

message Test {
    optional Nested nested = 1;
    optional Other other = 2;
    required string text = 3;
}

message Nested {
    required string nestedText = 1;
}