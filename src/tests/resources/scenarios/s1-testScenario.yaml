import:
  - "s0-before"

description: "test description"

before: "s0-before"

actions:
  - name: do-something
    description: "test something"
    method: POST
    data:
      userId: 22
    responseValidation:
    - "saved === true"

after: "s99-after"